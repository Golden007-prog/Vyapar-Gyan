# ============================================
# Public environment variables for GitHub Pages build
# These are safe to expose — they are client-side only
# ============================================

# Demo Mode — shows demo account card on login page
NEXT_PUBLIC_DEMO_MODE=false

# API Gateway endpoint (live AWS backend)
NEXT_PUBLIC_API_URL=https://6jseqwaaeh.execute-api.ap-south-1.amazonaws.com

# AWS Cognito (client-side auth — these are public by design)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=ap-south-1_jeKcCOzvw
NEXT_PUBLIC_COGNITO_CLIENT_ID=50upimcsem8nudea04l4ntld2f
NEXT_PUBLIC_AWS_REGION=ap-south-1

# GitHub Pages base path (set by CI workflow)
NEXT_PUBLIC_BASE_PATH=/Vyapar-Gyan

# WebSocket endpoint (live AWS backend — deployed by WebSocketStack)
NEXT_PUBLIC_WEBSOCKET_URL=wss://sze21qi00l.execute-api.ap-south-1.amazonaws.com/dev

# Razorpay test mode (public key only — safe to expose)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_SMPtcnanzcaSQb

# WebSocket API Gateway endpoint
NEXT_PUBLIC_WEBSOCKET_URL=wss://sze21qi00l.execute-api.ap-south-1.amazonaws.com/dev